mixin table-permission(records)
  //- Danh mục sản phẩm
  tr(data-name="products-category_view")
    td Xem
    each item in records 
      if (item.permissions.includes("products-category_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="products-category_create")
    td Thêm mới
    each item in records 
      if (item.permissions.includes("products-category_create"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="products-category_edit")
    td Chỉnh sửa
    each item in records 
      if (item.permissions.includes("products-category_edit"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="products-category_delete")
    td Xóa
    each item in records 
      if (item.permissions.includes("products-category_delete"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  //-  End danh mục sản phẩm
  
  //- Sản phẩm
  tr
    td(colspan="4")
      b Danh sách sản phẩm
  tr(data-name="products_view")
    td Xem
    each item in records 
      if (item.permissions.includes("products_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="products_create")
    td Thêm mới
    each item in records 
      if (item.permissions.includes("products_create"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="products_edit")
    td Chỉnh sửa
    each item in records 
      if (item.permissions.includes("products_edit"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="products_delete")
    td Xóa
    each item in records 
      if (item.permissions.includes("products_delete"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  //- end Sản phẩm

  //- Khôi phục sản phẩm
  tr
    td(colspan="4")
      b Khôi phục sản phẩm
  tr(data-name="restore_view")
    td Xem
    each item in records 
      if (item.permissions.includes("restore_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="restore_products")
    td Khôi phục sản phẩm
    each item in records 
      if (item.permissions.includes("restore_products"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  //- Khôi phục sản phẩm


  //- Nhóm quyền
  tr
    td(colspan="4")
      b Nhóm quyền
  tr(data-name="roles_view")
    td Xem
    each item in records 
      if (item.permissions.includes("roles_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="roles_create")
    td Thêm mới
    each item in records 
      if (item.permissions.includes("roles_create"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="roles_edit")
    td Chỉnh sửa
    each item in records 
      if (item.permissions.includes("roles_edit"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="roles_delete")
    td Xóa
    each item in records 
      if (item.permissions.includes("roles_delete"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)

  //- end nhóm quyền
  //- Phân quyền
  tr
    td(colspan="4")
      b Phân quyền
  tr(data-name="permission_view")
    td Xem
    each item in records 
      if (item.permissions.includes("permission_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="permission_edit")
    td Chỉnh sửa
    each item in records 
      if (item.permissions.includes("permission_edit"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  //- Phân quyền


  //- Danh sách tài khoản
  tr
    td(colspan="4")
      b Danh sách tài khoản
  tr(data-name="accounts_view")
    td Xem
    each item in records 
      if (item.permissions.includes("accounts_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="accounts_create")
    td Thêm mới
    each item in records 
      if (item.permissions.includes("accounts_create"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="accounts_edit")
    td Chỉnh sửa
    each item in records 
      if (item.permissions.includes("accounts_edit"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="accounts_delete")
    td Xóa
    each item in records 
      if (item.permissions.includes("accounts_delete"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  //- Phân quyền

   //- Danh sách tài khoản
  tr
    td(colspan="4")
      b Danh sách bài viết
  tr(data-name="blog_view")
    td Xem
    each item in records 
      if (item.permissions.includes("blog_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="blog_create")
    td Thêm mới
    each item in records 
      if (item.permissions.includes("blog_create"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="blog_edit")
    td Chỉnh sửa
    each item in records 
      if (item.permissions.includes("blog_edit"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="blog_delete")
    td Xóa
    each item in records 
      if (item.permissions.includes("blog_delete"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
    //- end danh sách bài viết

  //- Khôi phục bài viết
  tr
    td(colspan="4")
      b Khôi phục bài viết
  tr(data-name="restore_blog_view")
    td Xem
    each item in records 
      if (item.permissions.includes("restore_blog_view"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  tr(data-name="restore_blog")
    td Khôi phục bài viết
    each item in records 
      if (item.permissions.includes("restore_blog"))
        td.text-center
          input(type="checkbox" checked=true)
      else 
        td.text-center
          input(type="checkbox" checked=false)
  //- Khôi phục sản phẩm