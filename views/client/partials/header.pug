include ../mixins/sub-menu.pug

header.header
      .container
        .row.align-items-center
          .col-3.inner-logo
            a(href='/')
              img(src='/images/Snoopy Shop_transparent-.png', alt='Logo')
          .col-4
            .form-search
              form(
                action="/search"
                method="GET"
              )
                .form-group.d-flex.mb-8
                  input(
                    type="text"
                    placeholder="Nhập từ khóa..."
                    class="form-control"
                    name="keyword"
                    value=keyword
                  )
                  button(
                    type="submit"
                    class="btn btn-primary"
                  ) Tim
          .col-5
            .inner-menu
              ul
                li.sub-menu
                  a(href="/") Trang chủ
                  ul 
                    li
                      a(href="/") Trang chủ
                    li
                      a(href="/") Trang chủ
                li.sub-menu
                  -const pathProduct= "/products"
                  a(href=pathProduct) Sản phẩm
                    +sub-menu(layout, pathProduct)
                li.sub-menu
                  -const pathBlog= "/blog"
                  a(href=pathBlog) Bài viết
                    +sub-menu(layoutBlog, pathBlog)
                li 
                  a(href="/cart") Giỏ hàng (#{totalQuantity ? (totalQuantity) : 0})
